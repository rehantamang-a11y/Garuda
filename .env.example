# ── Firebase ──────────────────────────────────────────
# Keep Firebase for reliable audit storage (Firestore).
# Replace zero-backend EmailJS approach with Firebase for production use.
REACT_APP_FIREBASE_API_KEY=
REACT_APP_FIREBASE_AUTH_DOMAIN=
REACT_APP_FIREBASE_PROJECT_ID=
REACT_APP_FIREBASE_STORAGE_BUCKET=
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=
REACT_APP_FIREBASE_APP_ID=

# ── Google Sheets + Drive upload ───────────────────────
# Public Apps Script Web App URL. Must be the /macros/s/.../exec URL,
# not the workspace-scoped /a/macros/... URL.
REACT_APP_APPS_SCRIPT_URL=

# ── EmailJS ────────────────────────────────────────────
# Used to send audit report email on submission.
# Free tier: 200 emails/month. Upgrade if volume grows.
REACT_APP_EMAILJS_SERVICE_ID=
REACT_APP_EMAILJS_TEMPLATE_ID=
REACT_APP_EMAILJS_PUBLIC_KEY=

# ── WhatsApp ───────────────────────────────────────────
# Team WhatsApp group phone number (international format, no +).
# e.g. 447911123456 for a UK number.
REACT_APP_WHATSAPP_NUMBER=
